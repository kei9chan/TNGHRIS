import {build} from 'esbuild';import {mkdtemp,writeFile,rm} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {pathToFileURL} from 'node:url';
const dir=await mkdtemp(join(tmpdir(),'schedule-status-ui-'));try{const output=join(dir,'test.cjs');await build({stdin:{contents:`import React from 'react';import {renderToStaticMarkup} from 'react-dom/server';import assert from 'node:assert/strict';import RoleViewTable from './components/payroll/RoleViewTable';import EmployeePhoto from './components/employees/EmployeePhoto';
const employee={id:'fixture',name:'Sample Employee',role:'Employee'};const date=new Date('2026-09-07T00:00:00');const template={id:'shift',name:'Working shift',startTime:'09:00',endTime:'18:00',scheduleKind:'work',color:'blue'};const assignment={id:'assignment',employeeId:'fixture',date,shiftTemplateId:'shift'};const props={view:'grid',employeesByRole:{},employeesByArea:{},employees:[employee],weekDates:[date],operatingHours:null,validationStatus:{},assignments:[assignment],suggestedAssignments:[],templates:[template],shiftColorClasses:{blue:'blue'},dayStatuses:[],leaves:[],onOpenDetailModal:()=>{},onOpenDrawer:()=>{},onStatus:()=>{},isEditable:true};
const render=(extra={})=>renderToStaticMarkup(React.createElement(RoleViewTable,{...props,...extra}));const leave={id:'leave',employeeId:'fixture',startDate:date,endDate:date,status:'Approved',paid:true};assert.match(render({leaves:[leave]}),/Paid Leave/);assert.doesNotMatch(render({leaves:[leave]}),/>Working shift</);assert.match(render({leaves:[{...leave,paid:false}]}),/Unpaid Leave/);assert.match(render({leaves:[{...leave,status:'Rejected'}]}),/Working shift/);assert.match(render({dayStatuses:[{id:'status',employee_id:'fixture',work_date:'2026-09-07',tag:'rest'}]}),/Rest Day/);assert.match(render({dayStatuses:[{id:'status',employee_id:'fixture',work_date:'2026-09-07',tag:'skeletal'}]}),/09:00–18:00/);const avatar=renderToStaticMarkup(React.createElement(EmployeePhoto,{employeeId:'fixture',name:'Sample Employee'}));assert.match(avatar,/>SE</);assert.doesNotMatch(avatar,/pravatar|<img/);console.log('PASS: paid/unpaid/rejected leave precedence, Rest Day/Skeletal display, initials with no random photo.');`,resolveDir:process.cwd(),loader:'tsx'},outfile:output,bundle:true,platform:'node',format:'cjs',plugins:[{name:'no-network',setup(b){b.onResolve({filter:/supabaseClient$/},()=>({path:'stub',namespace:'stub'}));b.onLoad({filter:/.*/,namespace:'stub'},()=>({contents:'export const supabase={};',loader:'js'}));}}]});await import(pathToFileURL(output));}finally{await rm(dir,{recursive:true,force:true});}
